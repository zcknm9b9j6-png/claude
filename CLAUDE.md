# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current state

This repository is currently empty. It contains only this file and a placeholder
`README.md` (`# claude`). There is no source code, build system, dependency
manifest, test suite, or CI configuration yet.

There are no project-specific build, lint, run, or test commands to document at
this time. This section should be filled in once the project is scaffolded —
ideally with:

- How to install dependencies and build the project
- How to run the test suite, and how to run a single test
- How to run the linter/formatter
- How to run the application locally

## Architecture

No architecture exists yet. Once code is added, document the high-level structure
here — the "big picture" relationships between modules that require reading
multiple files to understand, rather than an exhaustive file listing.

## Conventions

- Work happens on feature branches (e.g. `claude/<topic>`), not directly on `main`.
- Update this file as the codebase grows so future Claude Code sessions stay oriented.
